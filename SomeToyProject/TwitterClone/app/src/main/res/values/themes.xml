<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TwitterClone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TwitterClone" parent="Base.Theme.TwitterClone" />
    <style name="AppTheme.NoActionBar" parent="Base.Theme.TwitterClone">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="StandardButton" parent="Base.Theme.TwitterClone">
        <item name="android:textColor" >@color/white</item>
        <item name="android:textSize" >@dimen/standard_text_size</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="android:background">@drawable/standard_button_rounded</item>
    </style>
    <style name="LinkText" parent="Base.Theme.TwitterClone">
        <item name="android:textColor">@color/blue</item>
    </style>
</resources>